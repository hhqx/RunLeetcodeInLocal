

from .RunLeetCodeInPycharm import StartTest

from .def_List import ListNode

from .def_Tree import TreeNode

# customed tools for debug
# import matplotlib.pyplot as plt
# import numpy as np

# def plot_text(coords, sz=5):
#     def add(x, y, str):
#         plt.text(x, y, str, size=sz, rotation=0.,
#                  ha="center", va="center",
#                  bbox=dict(boxstyle="round",
#                            ec=(1., 0.5, 0.5),
#                            fc=(1., 0.8, 0.8),
#                            )
#                  )
#
#     plt.figure()
#     for i, (x, y) in enumerate(coords):
#         add(x, y, f'No.{i}: ({x}, {y})')
#
#     mx, mn = np.array(coords).max(axis=0), np.array(coords).min(axis=0)
#     plt.axis([mn[0], mx[0], mn[1], mx[1]])
#
#     plt.show()

